[tool.poetry]
name = "llm-consensus"
version = "0.1.0"
description = ""
authors = ["Alex H"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
openai = "^2.28.0"
dotenv = "^0.9.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
